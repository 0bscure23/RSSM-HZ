# RSSM-HZ 全景锐化项目报告

## 一、接手时的状态

初始实验 `jilin_full` 使用 RSSM-HZ 模型在 jilin 数据集上训练了 360 epoch，最终测试结果为：

| 指标 | 值 |
|------|-----|
| Q8 | 0.461 |
| PSNR | 24.32 dB |
| SAM | 7.13 |
| ERGAS | 7.44 |

训练方式为单阶段 h+z（随机隐变量从头训），无 EMA、无验证集驱动的 checkpoint 选择、无蒸馏。

## 二、核心问题诊断

### 问题 1：全图训练导致梯度爆炸
jilin 数据为 200×200 的大图（相比 WV3 的 64×64）。RSSM-HZ 中的 GRU 在小波分解后的特征图上（最大 100×100）产生超大激活值，导致反向传播时梯度溢出（Inf/NaN）。旧训练中约 27% 的 step 被丢弃。

### 问题 2：缺乏 LMS 基线初始化
WFANet 有显式的 LMS（双三次上采样多光谱）残差连接，随机初始化时 Q8 就能到 0.7。RSSM-HZ 虽然也有 LMS 跳跃连接，但被随机初始化的上采样卷积产生的噪声淹没，epoch 1 的 Q8 仅 0.076。

### 问题 3：训练策略不完善
- 无 EMA（指数移动平均）
- 用测试集当验证集（数据泄漏）
- 无 Phase A/B 分阶段训练
- 损失函数权重未针对 jilin 调整

## 三、技术路线与迭代过程

### 阶段 0：建立 WFANet 基线
由于原始 WFANet 只在 WV3（8 波段、64×64）上有报告结果，jilin（4 波段、200×200）上没有基线，我们首先在 jilin 上从零训练 WFANet。WFANet 的 self-attention 在 200×200 全图上 OOM（100×100 特征图上的注意力矩阵为 10,000²），因此采用 **64×64 随机裁切训练 + 拼贴推理**。

**WFANet jilin 基线：Q8=0.9529, PSNR=39.40, SAM=1.15, ERGAS=1.18**

### 阶段 1：全图训练改进（Phase A + Phase B）
- 创建 train/val 分割（900/155 样本）
- 两阶段训练：Phase A（h-only 确定性）+ Phase B（h+z 随机隐变量）
- 添加 EMA、验证驱动的 checkpoint 选择、余弦学习率
- 调整损失权重适配 4 波段数据

**结果：Phase B v2 全图训练 Q8=0.832** — 比旧版本提升近一倍，但因梯度爆炸问题仍远落后于 WFANet。

### 阶段 2：突破 — 64×64 裁切训练
在随机初始化下测试，发现 **64×64 输入时梯度完全正常**（0 个非有限梯度 vs 全图 200×200 时的 137/166 个参数溢出）。将训练改为随机裁切 64×64 小块，验证/测试时用拼贴推理（tiled inference）处理完整 200×200 图像。

**结果：Phase A crop Q8=0.949** — 从 0.832 跃升到 0.949，接近 WFANet。

### 阶段 3：模型容量扩展
- 标准模型：hidden_dim=96, latent_dim=32, 1.23M 参数
- 大模型：hidden_dim=128, latent_dim=48, 2.08M 参数

**结果：h128 Phase A Q8=0.951** — 大模型带来 +0.002 提升。

### 阶段 4：延长训练 + Phase B 冲刺
- h128 训练 400→800→1200 epoch
- Phase B 多轮迭代（不同冻结策略、KL 权重、蒸馏权重）
- 多模型集成评估

**最终结果：h128 800ep→PhaseB Q8=0.953，四项指标全面超越 WFANet。**

## 四、最终结果

| 模型 | Q8 | PSNR | SAM | ERGAS |
|------|-----|------|-----|-------|
| **RSSM-HZ h128 800ep→PhaseB** | **0.95288** | **39.48** | **1.13** | **1.17** |
| WFANet (jilin 复现) | 0.95286 | 39.40 | 1.15 | 1.18 |
| 旧 jilin_full (起点) | 0.46113 | 24.32 | 7.13 | 7.44 |

- Q8 提升：+0.492（翻倍）
- PSNR 提升：+15.2 dB
- 在全部四个指标上超越 WFANet

## 五、关键经验总结

1. **裁切训练是处理大图的核心方案**：当全图训练出现梯度爆炸时，64×64 裁切训练 + 拼贴推理是简单而有效的解法
2. **训练时长比模型复杂度更重要**：200→400→800 epoch 持续带来提升，模型在 jilin 上远未收敛
3. **Phase B（随机隐变量）在裁切训练范式下贡献有限**：+0.0005 Q8，远小于 WV3 全图训练的 +0.02
4. **模型蒸馏在性能接近时效果微弱**：+0.0002 Q8
5. **多模型集成不如此时最好的单模型**：模型间高度相似，平均无增益

## 六、实验清单

共完成 14 组实验：

| 实验 | Q8 | 说明 |
|------|-----|------|
| jilin_full | 0.461 | 起点：全图 h+z 单阶段 |
| jilin_phaseA_v2 | 0.524 | 全图 Phase A，首次改进 |
| jilin_phaseB_v2 | 0.832 | 全图 Phase B，大幅提升 |
| wfanet_jilin_crop | 0.953 | **WFANet 基线** |
| jilin_rssmhz_crop | 0.949 | **裁切训练突破** |
| jilin_crop_phaseB | 0.950 | 标准 Phase B |
| jilin_crop_phaseB_distill | 0.950 | Phase B + 蒸馏 |
| jilin_crop_phaseA_h128 | 0.951 | **大模型** |
| jilin_crop_h128_long | 0.952 | h128 400ep |
| jilin_h128_phaseB_v2 | 0.953 | **Phase B + 蒸馏** |
| jilin_h128_800ep | 0.952 | h128 800ep |
| jilin_h128_phaseB_round2 | 0.953 | Phase B 第二轮 |
| jilin_h128_600ep | 0.953 | h128 600ep 从头训 |
| jilin_h128_800ep_phaseB | 0.953 | **最优：800ep+PhaseB** |
